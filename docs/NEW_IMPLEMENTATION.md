Instead of only one device I want to be able to use many devices (as many as the user wants), the new system goes from a normal 1 shoot thing to being able to automate many tasks at once.
What I want to create is a diagram like automation, using squares, arrows and diamond shape.

Squares will be mostly devices: cameras, lights, doors, sensors, devices; Each square can have its own command like "if *device_action*" (detects when some device makes an action, for chain activation), "activate", "deactivate", etc.

The arrows will be either connecting lines betwen squares or little tasks, for example: "connect", "wait x seconds", "else", "else if". They will also be able to hold variables.

The diamond shape will mostly be on situations like, receives input, if "if input is x" outputs 1, in one of its sides it outputs 1 (configurable),if "if input is x" outputs 0, in one of its other sides it outputs 1 or nothing, this form should be fully configurabel for functions.

The next feature is the ewelink devices, instead of holding only 1, after the first log in, it will have all the devices, at the time, stored for easy configuration by the user. Also prepare for ewelink devices to have more than "on" and "off" they can also have other functions in the remaining slots (2+ option devices).

The next feature is for the camera, the detection is working well, it recognises the cars and bikes, but a problem with only one picture as the whole data base for each vehicle sometimes (more than ofter) doesnt detect correctly as a "vehicle that should do this", so now when assigning it will start recording a section (from when the user clicks the button to the point where the user stops assignment), then let the user see it back, select the car/bike/person during the video, for example at second 1 it might recognize as car named "gt astra" but at second 4 the same car might not be recognized as the "gt astra" so the user clicks on it and it is added to the "gt astra" data base, after reviewing all the footage the data base now will be more complete for the car/vehicle/person of choice.